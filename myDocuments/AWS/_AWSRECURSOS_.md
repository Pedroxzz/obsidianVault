# Índice de Arquivos - Pasta AWS

Abaixo está uma tabela com links para todos os arquivos na pasta `AWS`.

```dataview
table file.name as "Nome do Arquivo", file.link as "Link"
from "AWS"
sort file.name asc
```
